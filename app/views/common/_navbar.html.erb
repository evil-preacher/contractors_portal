    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= image_tag("logo.png", class: "style_logo") %>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <% if current_user.company.nil? %>
                <li><%= link_to "Создать профиль компании", new_company_path %></li>
              <% else %>
                <li><%= link_to 'Профиль компании', company_path(current_user.company) %></li>
              <% end %>
              <% if can? :manage, User %>
                <li><%= link_to "Пользователи", users_path  %></li>
              <% end %>
            </ul>
            <p class="profile">
              <%= current_user.name %>


              <a href="my/users/sign_out", data-method="delete"><i class="fa fa-sign-out" aria-hidden="true"></i> Выход</a>
            </p>


            <% end %>
        </div>
      </div>
    </div>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= image_tag("logo.png", class: "style_logo") %>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              
            </ul>
            <p class="profile">
              <%= current_user.name %>

              <%= link_to '<i class="fa fa-sign-out" aria-hidden="true"></i> Выход'.html_safe, destroy_user_session_path, method: :delete %>
              <!-- <a href="my/users/sign_out", data-method="delete"><i class="fa fa-sign-out" aria-hidden="true"></i> Выход</a> -->
            </p>


            <% end %>
        </div>
      </div>
    </div>

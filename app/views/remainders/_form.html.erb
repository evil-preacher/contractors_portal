<%= form_for remainder do |f| %>
  <%= render 'common/errors', resource: remainder %>
  <form>
    <div class="form-group">
      <%= f.label :product_accounting_system_code, "Товар" %><br />
      <%= f.collection_select :product_accounting_system_code, Product.where(company_id: current_user.company_id), :accounting_system_code, :title, {}, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :remainder, "Количество" %><br />
      <%= f.number_field :remainder, step: 0.001, class: "form-control text-right", placeholder: '0,000' %>
    </div>

    <div class="actions">
      <%= f.submit "Создать", class: 'btn btn-primary'%>
    </div>
  </form>
<% end %>
